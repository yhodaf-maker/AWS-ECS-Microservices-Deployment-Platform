services:
  inventory:
    build: ./inventory-service

  orders:
    build: ./orders-service
    ports:
      - "8080:8080"
    environment:
      INVENTORY_URL: http://inventory:8080
    depends_on:
      - inventory